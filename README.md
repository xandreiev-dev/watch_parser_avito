# Avito Watch Parser

Парсер объявлений с Avito для умных часов.
Сделан как часть пайплайна для дальнейшего анализа и матчинга моделей.

Проект собирает объявления, очищает их и готовит к следующему этапу — сопоставлению моделей и записи в БД.

---

## Что делает парсер

Парсер:

* проходит по списку ссылок с Avito
* собирает объявления (цена, описание, продавец и т.д.)
* извлекает доставку (например: *"Доставка от 1 дня"*)
* фильтрует мусор
* убирает дубликаты
* сохраняет результат в Excel

---

## Логика работы

Парсер разбит на два отдельных прогона:

* **NEW** — новые товары
* **USED** — б/у товары

После запуска создаются 2 файла:

```
result/
 ├── Avito_ru_new.xlsx
 ├── Avito_ru_used.xlsx
```

---

## Структура проекта

Основные файлы:

* `parser_cls.py` — основной парсер
* `dto.py` — конфиг (AvitoConfig)
* `models.py` — модели данных (объявления)
* `config.toml` — настройки

---

## Конфигурация

Файл: `config.toml`

### Основное

```
new_urls = [ ... ]
used_urls = [ ... ]
```
 Два списка ссылок:

* новые объявления
* б/у объявления

---

### Ограничения и фильтры

```
min_price = 0
max_price = 99999999

keys_word_white_list = []
keys_word_black_list = []

seller_black_list = []
```

---

### Анти-бан (важно)

```
pause_between_links = 4
max_count_of_retry = 5
proxy_rotation_limit = 15
proxy_rotation_cooldown = 10
```

Что реализовано:

* случайные паузы между запросами
* retry при ошибках
* смена IP
* защита от повторных страниц

---

### Прокси

```
proxy_string = "login:pass@host:port"
proxy_change_url = "https://..."
```

---

## Что именно парсится

С каждого объявления:

* title (название)
* price (цена)
* url
* description
* sellerId
* rating
* delivery (очень важно)
* images

---

## Фильтрация

Фильтрация идет через `AdsFilter`:

* по цене
* по ключевым словам
* по продавцам
* по возрасту объявления

---

## Дедупликация

* по ID объявления
* по hash страницы

---

## Как запустить

### 1. Установить зависимости

```
pip install -r requirements.txt
```

---

### 2. Запуск (основной режим)

```
python parser_cls.py
```

---

## Telegram-уведомления

После каждого полного прогона парсер может отправлять короткий отчет в Telegram: магазин, время старта, длительность, количество объявлений, имена XLSX-файлов, разбивку по категориям `new` / `used` и короткую проверку качества ответа Avito (страницы, редиректы, пустой `catalog.items`).

Настройки берутся из `.env` в корне проекта. Пример без секретов есть в `.env.example`:

```env
TELEGRAM_NOTIFICATIONS_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_TIMEOUT=10
```

Если `TELEGRAM_NOTIFICATIONS_ENABLED=false` или токен/chat id не заполнены, парсер работает как раньше и ничего не отправляет.

Проверка отправки без запуска парсинга:

```powershell
$env:AVITO_TELEGRAM_TEST="true"; python parser_cls.py
```

Проверочный запуск с лимитом:

```bash
AVITO_TEST_FIRST_LINK=true AVITO_MAX_ADS=5 python parser_cls.py
```

В PowerShell:

```powershell
$env:AVITO_TEST_FIRST_LINK="true"; $env:AVITO_MAX_ADS="5"; python parser_cls.py
```

---

## ML-поля для проверки фейков

В XLSX-выгрузку добавляется поле `fake_grade`: текстовая оценка подозрительных формулировок в описании (`0`, `-1`, `-2`, `-3`). Поле не требует модели и БД.

Поле `fake_proba` считается отдельной CatBoost-моделью после матчинга, когда уже известны `watch_id`, модель/бренд и доступна история цен из БД. Его нужно подключать в downstream matcher перед записью в `g_shop_watch.fake_proba`.

---

## Важные моменты

### Защита от 429

Реализовано:

* random delay
* retry логика
* смена прокси
* остановка при дублях страниц

---

### Парсер НЕ делает матчинг

Он только подготавливает данные.

Дальше пайплайн такой:

```
Avito → raw.xlsx → cleaning → matching → DB
```

---

## Следующий этап проекта

После парсера:

1. Очистка данных
2. Матчинг моделей (`g_models_watch`)
3. Запись в БД:

   * `g_watch`
   * `g_shop_watch`
   * `g_watch_price`

---

## Дополнительно

Используется:

* MobileAPI — для получения моделей
* ChatGPT — для полу-ручного добора моделей

---

## Что уже сделано

* разделение NEW / USED
* стабильный парсинг без 429
* извлечение доставки
* фильтрация и очистка данных
* подготовка под matcher

---

## Планы

* улучшить матчинг моделей
* автоматическая запись в БД
* нормализация названий
* улучшение качества данных

---

## О проекте

Проект делается под реальную задачу — сбор и обработка данных по умным часам с Avito.

Основной упор:

* стабильность
* качество данных
* подготовка под дальнейший анализ

---

## 💬 Примечание

Проект основан на open-source решении, но был значительно переработан:

* добавлен batch parsing (new / used)
* улучшена устойчивость к блокировкам
* адаптирован под pipeline с БД и matcher
